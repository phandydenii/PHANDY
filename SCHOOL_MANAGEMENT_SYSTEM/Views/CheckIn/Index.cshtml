
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6">
            <h4 style="color: darkblue;">@Resources.Content.ManageCheckIn</h4>
        </div>
        @*<div class="col-md-6">
                <a href="#" class="btn btn-success btn-sm pull-right" onclick="Test()" style="margin-right:5px;"><span class="glyphicon glyphicon-plus-sign"></span>បន្ថែមបុគ្គលិក</a>
                <a href="#" class="btn btn-success btn-sm pull-right" onclick="Demo()" style="margin-right:5px;"><span class="glyphicon glyphicon-plus-sign"></span>គ្រប់គ្រងតួនាទី</a>
            </div>*@
    </div>
    <hr />
    <table class="table-hover table-bordered table-condensed table-striped" style="width:100%" id="tableCheckInDetail">
        <thead>
            <tr>
                <th>@Resources.Content.No</th>
                <th>@Resources.Content.Name</th>
                <th>@Resources.Content.CheckInDate</th>
                <th>@Resources.Content.RoomNo</th>
                <th>@Resources.Content.PrePaid</th>
                @*<th>ស្ថានភាព</th>*@
                <th style="width:15%">@Resources.Content.Action</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>

@*@Html.Partial("_PrintNewInvoice")*@
@Html.Partial("_CheckIn")
@Html.Partial("_Paydamages")
@Html.Partial("_PrintNewInvoice")

@section scripts{
    @Scripts.Render("~/Scripts/Functions/checkin.js")
    @Scripts.Render("~/Scripts/moment.min.js")
}

<script>
    var array = [];
   

    function Test() {
        
        $.ajax({
            url: "/api/paydemages/5/2023-01-12/2023-02-12",
            type: "GET",
            contentType: "application/json;charset=utf-8",
            datatype: "json",
            success: function (result) {
                //var id =result.id;
                for (var i = 0; i < result.length; i++) {
                    array[i] = result[i].id;

                    alert(result[i].id);
                }
                
            },
            error: function (errormessage) {
                toastr.error("No Record Select!", "Service Response");
            }
        });
    }

    function Demo() {
        for (var j = 0; j < array.length; j++) {
            console.log(array[j]);
        }

    }
</script>
 